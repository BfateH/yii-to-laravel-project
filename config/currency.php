<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Базовая валюта
    |--------------------------------------------------------------------------
    |
    | Код валюты, относительно которой рассчитываются курсы.
    | Провайдеры должны поддерживать эту валюту или быть адаптированы.
    |
    */
    'base_currency' => env('CURRENCY_BASE', 'RUB'),

    /*
    |--------------------------------------------------------------------------
    | Провайдер курсов
    |--------------------------------------------------------------------------
    |
    | Имя провайдера, используемого по умолчанию.
    | Должно соответствовать ключу в фабрике провайдеров.
    |
    */
    'default_provider' => env('CURRENCY_PROVIDER', 'cbr'),

    /*
    |--------------------------------------------------------------------------
    | Время жизни кэша (в минутах)
    |--------------------------------------------------------------------------
    |
    | Время, на которое курсы кэшируются.
    |
    */
    'cache_ttl' => env('CURRENCY_CACHE_TTL', 60),

    /*
    |--------------------------------------------------------------------------
    | Попытки повтора
    |--------------------------------------------------------------------------
    |
    | Количество попыток повтора при ошибке получения курсов.
    |
    */
    'retry_attempts' => env('CURRENCY_RETRY_ATTEMPTS', 3),

    /*
   |--------------------------------------------------------------------------
   | Задержка между повторами (в миллисекундах)
   |--------------------------------------------------------------------------
   |
   | Начальная задержка между повторами (будет увеличиваться экспоненциально).
   |
   */
    'retry_delay' => env('CURRENCY_RETRY_DELAY', 1000),
];
